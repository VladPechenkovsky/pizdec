Use `$pizdec` in TOTAL mode to audit the authorized isolated target at `<TARGET_PATH>`. This evaluation runtime has a hard six-first-party-file read limit for the current turn and supports native continuation in a later turn. Inventory the complete target first. When the six-file limit is reached, return only the exact `AUDIT_CONTINUE` capsule required by PIZDEC. Do not return a human terminal report, run target code, use the network, or change any file.
